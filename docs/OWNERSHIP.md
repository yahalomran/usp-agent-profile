# USP Agent platform profile — ownership

Canonical profile URI (platform identity; do not change lightly):

`https://profile.usp-agent.dev/platform-profile.json`

| Asset | Owner (Steps 0–9) | Owner after Step 10 |
|-------|-------------------|---------------------|
| Domain `usp-agent.dev` | Ran Yahalom / Wix DNS for `usp-agent.dev` | Unchanged (domain stays on Wix nameservers) |
| GitHub repo `usp-agent-profile` | [`yahalomran`](https://github.com/yahalomran) (personal public) | Public Wix org (transfer; TBD login) |
| GitHub Pages site | `yahalomran.github.io` | `<org>.github.io` after transfer |
| Profile document content | `yahalomran/linkusp-cli` `platform_profile_doc()` (SoT) | Same SoT; this repo only publishes generated bytes |

## Who can transfer what

- **Domain transfer / registrar changes:** domain registrant for `usp-agent.dev` (Wix Domains).
- **Repo transfer:** current GitHub owner admin (`yahalomran` until Step 10; then the public Wix org owners).
- **DNS `profile` CNAME:** whoever can edit Wix DNS for `usp-agent.dev` (must point at `<owner>.github.io`).

## Rotation of neither

This document is **keyless**. There are no profile signing keys to rotate. PoP keys are per-credential and ephemeral in the client; they are never published here.

Update this file again when Step 10 (org transfer) completes.
